import React from 'react';
import Cart from '../Cart/Cart';

const ChoooseProduct = (props) => {
    console.log(props);

    return (
        <div>
            {/* <h4>{props.item.name}</h4> */}
        </div>
    );
};

export default ChoooseProduct;